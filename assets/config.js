/* إعدادات قاعدة البيانات (Supabase)
   ضعي هنا رابط المشروع والمفتاح العام (anon public key) من:
   Supabase ← Project Settings ← API
   إذا تُركت فارغة تعمل اللعبة على الجهاز فقط (بدون قاعدة بيانات). */
window.MAAMOON_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
